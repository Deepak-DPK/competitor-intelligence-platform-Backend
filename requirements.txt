# ==============================================================
# requirements.txt
# AI Hotel Booking Competitor Intelligence Platform — Backend
# ==============================================================
# Pin exact versions to guarantee reproducible builds on Render.
# Update via: pip-compile requirements.in  (or manage manually)
# ==============================================================

# ----------------------------------------------------------------
# Web Framework
# ----------------------------------------------------------------
fastapi==0.115.5
uvicorn[standard]==0.32.1          # includes uvloop + httptools
python-multipart==0.0.12           # required for form/file uploads

# ----------------------------------------------------------------
# Pydantic & Settings
# ----------------------------------------------------------------
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0             # required for pydantic EmailStr fields

# ----------------------------------------------------------------
# Database — SQLAlchemy 2 + asyncpg driver
# ----------------------------------------------------------------
sqlalchemy==2.0.36
asyncpg==0.30.0                    # async PostgreSQL driver
psycopg[binary]==3.2.10             # sync driver for Alembic CLI

# ----------------------------------------------------------------
# Migrations
# ----------------------------------------------------------------
alembic==1.14.0
mako==1.3.6                        # Alembic template engine

# ----------------------------------------------------------------
# Authentication / Security
# ----------------------------------------------------------------
pyjwt==2.10.1
bcrypt==4.2.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# ----------------------------------------------------------------
# Supabase
# ----------------------------------------------------------------
supabase==2.10.0

# ----------------------------------------------------------------
# AI / LLM
# ----------------------------------------------------------------
google-generativeai==0.8.3

# ----------------------------------------------------------------
# Web Scraping / Monitoring
# ----------------------------------------------------------------
playwright==1.49.0
beautifulsoup4==4.12.3
lxml==5.3.0                        # fast BS4 parser
httpx==0.27.2                      # async HTTP client (also used by FastAPI tests)
firecrawl-py==4.32.1

# ----------------------------------------------------------------
# Utilities
# ----------------------------------------------------------------
python-dotenv==1.0.1
tenacity==9.0.0                    # retry logic
orjson==3.10.12                    # fast JSON serialiser
slowapi==0.1.9                     # rate limiting
cachetools==5.3.3                  # in-memory caching

# ----------------------------------------------------------------
# Testing
# ----------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.24.0
anyio==4.7.0
aiosqlite==0.20.0
